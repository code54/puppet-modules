# [RVM](http://rvm.beginrescueend.com) Module

This module is heavily inspired by [Puppet pip](https://github.com/rcrowley/puppet-pip) module.

You can test in a Vagrant VM:

    $ cd test
    $ vagrant up
